import Text from "../basicsComponents/Text";

function TextTask({ task }) {
    return (
        <div>
            {/* <Button text="додати" onClick={() => handleAddText("")} /> */}
        </div>
    );
}

export default TextTask;
